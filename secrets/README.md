# Secrets

This folder is for local secrets (API keys, access keys, etc.).

**Nothing in this folder is committed to Git** (except this README and `.gitkeep`).

## Contents expected locally

- `adaptivelearning_accessKeys.csv` - AWS CLI access keys (IAM user)
- Any other local-only credentials

## Important

Never commit real credentials. If you accidentally do, rotate them immediately.
